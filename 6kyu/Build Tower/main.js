
function towerBuilder(nFloors) {
    let digits = nFloors * 2 - 1
        tower = []
         
    for(let x = 1; x < nFloors; x++){
        let stars = x * 2 - 1 * '*'
        return stars
        tower.push(stars)
    }
    console.log(stars)
  } 

  towerBuilder(8)